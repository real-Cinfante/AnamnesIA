# URL pública del backend Flask (dev local)
NEXT_PUBLIC_API_URL=http://localhost:5001

# Solo en producción / server-side: URL interna del backend en Railway
BACKEND_URL=https://tu-backend.railway.app

# Clave compartida entre Next.js y Flask (nunca exponer al browser)
BACKEND_API_KEY=clave_secreta_entre_flask_y_nextjs
